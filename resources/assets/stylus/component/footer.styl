footer {
      z-index: -100;
      align-items: center;
      justify-content: center;
      display:flex;
      text-align: center;
}
.footerInfo {
      text-transform: uppercase;
      font-weight: 800;
      a {
            color: inherit;
            text-decoration: none;
      }
}